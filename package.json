{
  "name": "studio-runner",
  "version": "1.0.0",
  "type": "module",
  "description": "Voice-to-memo + DeepSeek-backed track-state assistant for music production sessions. Transcribes speech via whisper-cpp, captures screenshots, records DAW audio, consolidates a live track-state markdown via DeepSeek, and answers spoken questions via `say`.",
  "dependencies": {
    "@julusian/midi": "^3.6.1"
  },
  "trustedDependencies": [
    "@julusian/midi"
  ]
}
